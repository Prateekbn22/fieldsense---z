# FieldSense-Z project configuration
#
# This file is intentionally minimal during repository initialization.
# Zephyr features such as logging, shell, I2C, and sensor support will be added
# only when the matching milestone requires them.
